# Local dev only — points the frontend to your local FastAPI backend.
# Leave this unset (or delete this file) in production;
# Railway serves both frontend and API from the same origin.
NEXT_PUBLIC_API_URL=http://localhost:8000
